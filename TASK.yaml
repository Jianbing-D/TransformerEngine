Description: "Tasks for Linear-Cross-Entropy fusion in TransformerEngine"
StatusDescription: "When a task is complete, the status field would be 'completed'"

Tasks:
  - id: 1
    name: "Understand the Linear-Cross-Entropy fusion code structure in TransformerEngine"
    status: "completed"
    material:
      - ./tests/pytorch/test_linear_cross_entropy.py contains the test code for Linear-Cross-Entropy fusion in TransformerEngine.
      - ./3rdparty/cutlass/ contains the critical components for Linear-Cross-Entropy fusion in TransformerEngine, cutlass and cuteDSL.
    steps:
      - read those materials in depth, write a detailed report about the code structure and data flow of Linear-cross-entropy fusion, including tensor shapes, data type.
      - record critical components and APIs that used in the Linear-cross-entropy fusion, including APIs from triton, APIs from cutlass, etc.
      - write a detailed report to KNOWLEDGE.md, with the learnings and insights you have gained. Try to separate different concepts and parts in different sections.
      - write a detailed plan to PROGRESS.md first, with TODO-list for each step. Then follow the plan to implement the task.
    permissions:
      - You MUST NOT execute git commit command in any way.
      - You may start sub-agents to help you with the task.
      - You have access to all files in the current directory.
  - id: 2
    name: "Use static tile scheduler in fwd_mainloop.py"
    status: "completed"
    material:
      - ./transformer_engine/common/cutedsl/linear_cross_entropy/blackwell/fwd_mainloop.py contains the forward mainloop kernel implementation.
      - ./transformer_engine/common/cutedsl/linear_cross_entropy/scheduler.py contains the static tile scheduler implementation.
      - ./Makefile contains the test command for the kernel.
    steps:
      - read the materials in depth, understand how the kernel works with the current tile indices.
      - apply the static tile scheduler to the kernel, make the kernel run in a persistent way.
      - write a detailed report to KNOWLEDGE.md about the static tile scheduler and how it works.
      - write a detailed plan to PROGRESS.md first, with TODO-list for each step. Then follow the plan to implement the task.
      - when you updated code, use make unit-test-1gpu to make sure the kernel works correctly.
    permissions:
      - You MUST NOT execute git commit command in any way.
      - You may start sub-agents to help you with the task.
      - You have access to all files in the current directory.
  - id: 3
    name: "Brain storm better implementation for linear-cross-entropy fusion"
    status: "completed"
    steps:
      - analyze the linear-cross-entropy fusion algorithm with mathmetical analysis.
      - based on your understanding of the linear-cross-entropy fusion problem, brain storm better implementation for the problem.
      - current implementation is trying to hit a balance between performance and memory usage.
      - You must take data types into account, as it will affect the calculation precision. 
      - then consider different ways to implement it, mapping those concepts with cuteDSL APIs.
      - write a detailed report to KNOWLEDGE.md about the different implementations and their trade-offs.
      - write a detailed plan to PROGRESS.md first, with TODO-list for each step. Then follow the plan to implement the task.
  - id: 4
    name: "Optimize the backward kernel bwd_partial_dlogits.py"
    status: "completed"
    description:
      - bwd_partial_dlogits.py contains the backward kernel implementation to calculate the partial d_logits tensor.
      - generally speaking, bwd_partial_dlogits kernel is only a GEMM kernel with fused epilogue operations.
      - common used GEMM optimizations could be applied to that kernel, including:
        - use TMA for write out of d_logits tensor.
        - use persistent scheduler to make the kernel run in a persistent way.
        - use 2-CTA MMA instructions to improve the performance.
    material:
      - ./transformer_engine/common/cutedsl/linear_cross_entropy/blackwell/bwd_partial_dlogits.py contains cuteDSL kernel for bwd_partial_dlogits.
      - ../CUTLASS/python/CuteDSL/ contains the source code of cuteDSL APIs.
      - ../CUTLASS/examples/python/CuTeDSL/blackwell/ contains the examples of cuteDSL APIs, including GEMM kernel implementations.
      - ./transformer_engine/common/cutedsl/linear_cross_entropy/scheduler.py contains my static tile scheduler implementation.
    steps:
      - read my kernel (bwd_partial_dlogits.py) in depth, understand how it works.
      - ready the examples of cutlass GEMM kernel implementations in depth, understand how they work. Write a detailed report about those example GEMM kernel implementations to KNOWLEDGE, including their optimization techniques.
      - write a thorough plan about how to apply those optimization techniques to my kernel, including the trade-offs and performance improvements.
      - write a detailed plan to PROGRESS.md first, with TODO-list for each step. Then follow the plan to implement the task.
      - remember to update KNOWLEDGE.md and PROGRESS.md when you completed each step, and each trial.
    permission:
      - YOU MUST NOT execute git commit command in any way.
      - You have access to all files in the current directory.
      - You have access to all files in the ../CUTLASS directory.
      - You may start sub-agents to help you with the task.
      - When you implemented some code, use `make unit-test-1gpu` and `make unit-test-4gpu` to verify the correctness of the kernel.
      - When you verified the correctness of the kernel, use `make ncu-bwd-cli` to profile the kernel performance.
      - `bwd_partial_dlogits.log` is the profile log of the kernel, you can use it to analyze the kernel performance.
      - Generally speaking, the lower the Duration, the better the performance.
  - id: 5
    name: "Fix bugs in bwd_partial_dlogits.py"
    status: "completed"
    steps:
      - do this task in an iterative way, unit-test-1gpu -> locate errors -> fix errors -> unit-test-1gpu -> ...
      - the warp-specialization structure is basically correct right now. You have have to make the c tensor partitioned correctly. 
      - use mbarriers for synchronization between epi warps and store warp.
      - write a detailed plan to PROGRESS.md first, with TODO-list for each step. Then follow the plan to implement the task.
    permission:
      - DON'T look for cutlass reference implementation. Just fix bugs.
      - YOU MUST NOT execute git commit command in any way.
      - You may start sub-agents to help you with the task.
      - You have access to all files in the current directory.
      - When you implemented some code, use `make unit-test-1gpu` and `make unit-test-4gpu` to verify the correctness of the kernel.
  - id: 6
    name: "Make sure the static scheduler issues the number of SMs at max"
    status: "completed"
    description:
      - currently, the schedule would launch 2 * SM grid for a kernel, which is not the intention.
    steps:
      - read the materials in depth, understand how the static scheduler works.
      - write a detailed plan to PROGRESS.md first, with TODO-list for each step. Then follow the plan to implement the task.
      - when you implemented some code, use `make unit-test-1gpu` and `make unit-test-4gpu` to verify the correctness of the kernel.
      - When you verified the correctness of the kernel, use `make ncu-bwd-cli` to profile the kernel performance.
      - the lower the Duration, the better the performance.
  - id: 7
    name: "Move tensor partitions outside the while loop in bwd_partial_dlogits.py"
    status: "completed"
    description:
      - move those tensor partitions outside the while loop in bwd_partial_dlogits.py.
      - for example, the gA and gB tensor partitions should be moved outside the while loop.
      - and then just use the correct index to access the tensor partitions in the while loop.
      - finally, there will be no more tensor partitions inside the while loop.
    steps:
      - write a detailed plan to PROGRESS.md first, with TODO-list for each step. Then follow the plan to implement the task.
      - when you implemented some code, use `make unit-test-1gpu` and `make unit-test-4gpu` to verify the correctness of the kernel.
  - id: 8
    name: "Analyze the performance bottleneck of the backward pass"
    status: "completed"
    description:
      - the benchmark problem size is ((1, 4096), 129280, 7168), and current backward latency is 15.49ms.
      - current design is to have 42 splits, and each split composes of a cuteDSL kernel for materializing dlogit, a GEMM kernel for accumulating of d_hidden in FP32 to ensure precision, and a GEMM kernel for calculating partial d_weight.
      - backward pass can be found in ./transformer_engine/pytorch/cutedsl/linear_cross_entropy_entry.py in line 345
      - help me analyze the backward algorithm, and identify the performance bottleneck.
      - another design is to have two separate kernels to calculate the d_hidden and d_weight, respectively.
      - which means no more materializing of d_logits tensor in the backward pass.
    steps:
      - read relevant knowledge and materials in depth, understand the backward algorithm and the performance bottleneck.
      - write a detailed plan to PROGRESS.md first, with TODO-list for each step. Then follow the plan to implement the task.
      - finnaly, you shall produce a detailed analysis report to knowledge.
  - id: 9
    name: "Apply 2-CTA MMA instruction to fwd_mainloop.py"
    status: "completed"
    description:
      - understand how the bwd_partial_dlogits.py applies 2-CTA MMA instruction in depth.
      - then apply the same technique to fwd_mainloop.py.
    steps:
      - read bwd_partial_dlogits.py in depth, understand how it applies 2-CTA MMA instruction.
      - write a detailed plan to PROGRESS.md first, with TODO-list for each step. Then follow the plan to implement the task.
      - when you implemented some code, use `make unit-test-1gpu` and `make unit-test-4gpu` to verify the correctness of the kernel.
    permission:
      - YOU MUST NOT execute git commit command in any way.
      - You have access to all files in current directory.
      - Don't ask for permission unless you want to make modifications.